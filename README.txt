My solution takes a file as an input, I have produced some sample data as an example. 

To run my code you'll need to run from the command line including the name of a file that is present at this level of the directory. 